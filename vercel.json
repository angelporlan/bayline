{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm run postinstall -w @bayline/api && npm run build -w @bayline/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
